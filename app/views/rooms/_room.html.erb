<a href="<%= room_url(room) %>" class="list-group-item list-group-item-action p-3 lh-sm <%= room_active?(room) %> overflow-hidden">
  <div class="d-flex w-100 align-items-center justify-content-between">
    <strong class="mb-1"><%= room.name %></strong>
    <small class="text"><%= formatted_message_creation_dt_for_room(room) %></small>
  </div>

  <div class="mb-1 small text-truncate"><%= last_message_text(room) %></div>
</a>
